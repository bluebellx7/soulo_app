# 0–5 现有功能完善记录

日期：2026-09-05。基线：`95dbee1`。范围：检查并完善现有功能，不新增完整模块，不修改营销版本号和工程能力。

## 修改与对应问题

| 阶段 | 已确认问题 | 本轮处理 |
| --- | --- | --- |
| 0：体验/启动基础 | 广告订阅服务初始化时，即使规则未变也更新缓存签名，造成不必要的编译缓存失效；部分小图标触控范围不足 | 仅在规则内容变化时更新签名；扩大下载操作和站点帮助按钮触控范围 |
| 0：截图响应 | 截图流程无限等待网页动画帧，页面不再返回帧回调时无法继续 | 两处等待加入 250ms 回退；模拟停止帧回调，验证截图完成及底部像素 |
| 1：自定义 | 自动填充名称被当作手动输入，修改域名后不再更新；切离预选自定义分组仍加入旧组；搜索模板只校验占位符；派生主页丢失端口；保存人为等待 300ms | 区分用户编辑和自动填充；以当前分组选择为准；新增/编辑统一 HTTP(S) URL 校验、去首尾空白并保留端口；移除保存延时 |
| 1：编辑反馈 | 内置站点名称允许输入但保存时忽略 | 名称字段只读，避免显示可编辑却不保存 |
| 1：错误反馈 | 新网址校验在界面检查时显示了系统错误码 | 改为“请输入以 http:// 或 https:// 开头的有效网址”，补齐 50 种语言及同语言兼容目录 |
| 2：JS 扩展 | 清空显式站点匹配规则后，保存会自动扩大为所有站点；编辑保存未执行导入时的脚本大小限制 | 禁止空范围保存；按 UTF-8 字节执行相同大小限制；网址规则编辑关闭自动大写和纠错；验证编辑后仍保留启停及脚本存储 |
| 3：翻译 | 翻译期间网页改变原文字段，旧译文仍覆盖；恢复原文/再次翻译会覆盖网页新内容；遗漏部分 contenteditable 写法 | 只替换与快照一致的文本，只恢复仍是本次译文的节点；跳过实际可编辑区域；内容全部过期时报告失败 |
| 4：广告订阅 | 更新过程结束时用旧快照覆盖用户刚切换的开关；HTML 登录/错误页可能替换有效规则；站点专属元素规则未计入数量 | 异步返回后按 ID 更新当前记录，保留选择；拒绝 HTML 响应并保留原规则；按结构化规则计数 |
| 5：下载/文件入口 | 暂停任务不保留文件名；迟到的恢复/暂停回调可能污染已完成或取消任务；失败转前台重试留着完成时间；清除已完成下载会直接删除文件 | 暂停任务保留目标文件名；保护完成/取消状态；保留合法失败重试并清理旧完成时间；批量删除增加确认，未有可清理项目时隐藏入口 |

## 验证范围

- 自定义：有效 HTTP(S)、本地端口、国际域名、前后空白、缺少占位符、非法 scheme、域名空格。
- JS：清空规则不扩大范围且失败不修改原脚本；停用状态与存储保留；多字节超限脚本被拒绝。
- 翻译：真实 WKWebView 内验证 DOM 保留、原文恢复、可编辑区保护、翻译前后动态修改、全部文本过期。
- 广告：用本地 URLProtocol 固定响应验证请求期间切换开关、HTML 错误页回滚、站点规则计数、重启缓存签名稳定。
- 下载：暂停后重启和同名任务、迟到回调、完成文件完整性、失败转前台重试状态；保留原有暂停/恢复/取消测试。
- 最终逻辑回归：243 项 XCTest，0 失败（新增 12 项）。普通长截图 2.169 秒；停止帧回调的故障样本 1.978 秒。结果包：`/tmp/soulo-website-qa/PolishVerifiedTests.xcresult`。
- 复查过程保留记录：首次全量 242 项通过，但长截图耗时 154.561 秒；两次 60 秒限时复跑超时；原基线对照 3.109 秒、临时诊断版本 2.739 秒。未将这些波动直接归因为环境；发现无边界帧等待后增加回退并通过最终全量回归。诊断日志已移除。
- 最终 UI 文案调整后重新构建成功；`859 keys × 50 locales` 严格检查通过，商店生成文件 50 语言一致性检查通过，`git diff --check` 通过。
- 模拟器实操：连续修改 URL 自动名称更新；手动名称不被覆盖；非法 scheme 无法保存；有效模板只保存一条且主页端口保留；清理确认可以取消，文件内容仍完整并可预览；脚本范围清空后保存按钮禁用。检查了深色站点/下载页面及浅色脚本编辑页面。
- 操作截图：`/tmp/soulo-website-qa/download-clear-confirmation.png`、`/tmp/soulo-website-qa/script-empty-scope.png`。测试仅使用专用模拟器与本地样本。

## 保留的边界

- 没有实测依据时不改启动调度，也不宣称已经达到路线图中的真机毫秒级目标。
- 没有把用户主动安装的扩展通知能力直接删除；产品推送与主动扩展操作分开审查，避免改变既有扩展行为。
- 广告解析器仍是现有语法子集及容量限制；本次没有替换引擎或承诺完整 ABP 兼容。
- Apple 翻译模型需要真机验证；模拟器的 DOM 测试不代表语言模型、离线语言包和所有站点均已验收。
- 文件管理继续复用当前下载服务与系统文件夹入口。本次不新增压缩引擎、Wi-Fi 服务、播放器或阅读器。
