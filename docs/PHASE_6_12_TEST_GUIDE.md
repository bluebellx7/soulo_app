# P0–P12 改动与测试指南

本文汇总 P0–P5 已有功能完善与 P6–P12 新增工具的改动和测试方法，沿用原文件名。P0–P5 完善记录对应已有提交 `d8bd98a`；P6–P12 修改及本次完善纳入 1.1.5（20），按用户要求一起提交推送。

## 1.1.5（20）：全屏修复与统一主题色

- **全屏入口闪退**：连接的 iPhone 17 / iOS 26.6 上两份崩溃日志均指向 `fullscreenQuickMenu` 构建时的 Swift 泛型类型解析和栈保护越界。将菜单与按钮拆为独立具体 View，减少父页面中嵌套类型展开和栈占用。模拟器已实点连续 6 次展开/关闭及退出；同一真机的独立测试程序已完成 30 次菜单呈现/移除，无崩溃。验收：进入全屏，反复轻点顶部把手、关闭菜单；测试复制链接、缩放、返回及退出全屏，再测试下拉退出。
- **统一蓝色强调色**：`AppTheme` 统一提供系统动态蓝色，`AccentColor` 保留对应的蓝色资源兜底；SwiftUI、UIKit 菜单图标和应用注入的扩展安装按钮共用主题色入口。主题入口为 `Color.themePrimary` / `AppTheme`，本次未增加用户自定义颜色的设置。验收：深色/浅色下检查壁纸页 Nature 标签与刷新按钮、资料库 Tab、文件操作、设置开关、地址编辑及脚本入口，操作强调色应一致。
- **工具菜单**：直接展示阅读、媒体、保护操作，组间横线分隔；文件页右上角只保留导入与 Wi-Fi 传输，压缩从文件多选后的底部操作栏进入。
- **版本和商店资料**：App、Widget、分享扩展统一为 1.1.5（20）；50 语言更新说明、推广文案、描述已同步，名称/副标题/关键词沿用仍适用的内容，全部 300 个字段已校验。JSON 由源目录重新导出，版本一致性及 50 语言严格检查通过，导出器未报告零宽字符警告。

## 2026-09-05：文件、阅读、传输与设置完善

| 改动 | 如何测试 |
| --- | --- |
| 无后缀/后缀错误的图片按内容识别；预览临时补上正确后缀，原文件名不变 | 在资料库 → 文件，打开无后缀 JPEG/PNG，应该显示图片；返回后文件名保持原样。下载列表里的预览也使用相同处理。 |
| 文件名最多两行，中间省略，末尾和类型/大小单独保留；不同格式显示不同图标 | 放入长文件名、图片、视频、音频、PDF、书籍和压缩包，检查文件名不会撑成多行，类型一眼可辨。 |
| 复选框用于多选；底部显示压缩、分享、删除及取消选择；右上角菜单移除重复的压缩入口 | 右上角菜单应只有导入文件、Wi-Fi 传输。勾选两个测试文件，底部三个动作都应可用。压缩打开设置页；分享打开系统分享面板；删除先取消，原件保留，再用测试文件确认删除。单个文件可左滑或长按删除。文件夹不参与批量删除。 |
| 悬浮播放器可收起到左右侧边，点击展开；拖到屏幕边缘也会吸附 | 播放音视频后返回列表，点浮窗的小箭头，浮窗应变成侧边入口；点入口恢复。再拖向边缘、上下移动，确认入口不越界，收起时播放继续。 |
| Wi-Fi 电脑页重新排版，文件类型图标、上传区和下载列表分开 | 手机开启传输，在电脑打开地址。检查中英文、小窗口，拖入文件、选择文件、取消上传及重名错误提示；下载内容应一致。 |
| 手机端新增“需要配对码”开关、传输完成记录和复制按钮 | 开启传输前关闭“需要配对码”，电脑应直接连接；停止后重新开启配对码，错误码应被拒绝。地址和配对码右侧各点复制，显示“已复制”，粘贴核对。免配对码时不显示代码及其复制按钮。上传成功后手机显示“手机已收到”；电脑下载后显示“已发送到电脑”。 |
| 阅读默认字号 18、行高 1.6；改变样式不重新加载正文 | 新安装检查默认值；老用户直接覆盖安装后，已有字号/行高应保留。阅读到中部再调整字号、行高、主题，正文不闪回开头。 |
| 小说正文清理与分页增强，同一章不重复显示标题 | 打开下面的测试网址 → 网页工具 → 阅读模式，点“继续阅读”。第 260 章应依次读完三个分页，再进入第 261 章；正文不包含举报链接和“本章未完”提示。返回后原网页仍可使用。 |
| 网页工具直接显示全部操作，阅读、媒体、保护三组之间用横线分隔，不再进入分组子菜单 | 打开网页工具应直接看到操作；逐组检查阅读/翻译/缩放/查找、媒体倍速/资源/截图、脚本/广告/站点保护入口，功能应与之前一致。自定义工具栏中的直接入口仍可使用。 |
| 广告过滤页突出总开关，订阅行只保留名称、开关、数量/日期；更新按钮在订阅标题右侧 | 开关总过滤、切换单项订阅、更新规则；失败信息单独显示。检查设置、隐私、广告过滤、语言、站点、分组、外部跳转和下载页均使用行内小标题；从设置进入广告页不再重复显示“完成”。 |
| 首次记录使用时间满 7 天后，结束浏览返回首页请求一次系统评价 | 正常使用满 7 天后，浏览并返回首页；若首页没有其他弹窗，约 2 秒后请求系统评价。同一次安装不再自动请求。自动化已覆盖第 7 天边界、重复启动和只请求一次；系统可能不展示弹窗，不能以“没弹出”直接判断失败。 |
| 官网首页前景手机换为提供的山景截图 | 打开本地官网，切换中文/英文并缩小到手机宽度；前景手机均显示新的山景截图，后景浅色手机保持原图。 |

**小说实测地址**：https://m.chinawuxi.net/du/12541565/43953077.html 。已通过实际 WKWebView 验证：`43953077.html → 43953077_2.html → 43953077_3.html → 43979272.html`，即第 260 章三个分页到第 261 章。该站带有广告脚本，后续章节优先不运行网页脚本读取静态正文；无正文时才尝试动态加载，同时限制同源跳转。

阅读实现参考 [Mozilla Readability](https://github.com/mozilla/readability) 与 [Firefox iOS 阅读服务](https://github.com/mozilla-mobile/firefox-ios/blob/main/firefox-ios/Client/Frontend/Reader/ReadabilityService.swift)，保留原 DOM、在独立脚本环境提取和清理正文。评价展示由 [StoreKit RequestReviewAction](https://developer.apple.com/documentation/storekit/requestreviewaction) 决定；本轮没有增加自制星级弹窗。开发构建可展示系统测试弹窗，TestFlight 中此 API 不显示弹窗。

新增默认值只在对应偏好不存在时使用，不重置老用户设置。文件删除不可恢复，请用测试文件验收；Wi-Fi 接收记录是页面内反馈，未增加推送通知。官网资源保存在 `website/images/search-home.png`，与给定链接下载的图片一致。

## 本次 UI、按钮与默认设置

| 改动 | 如何测试 |
| --- | --- |
| 资料库改为收藏、搜索历史、下载、文件、书库五项导航；增加选中态、留白和空状态插画 | 逐项切换，文件应独立显示；进入子目录或文件预览后返回，仍回到原目录。浅色、深色各检查一次。 |
| 历史清理、下载文件夹、文件菜单、书籍导入操作统一放在右上角 | 历史点清理后取消，记录应保留；下载点文件夹应切到文件 Tab；书库「＋」应打开导入选择器。 |
| 图标按钮以首页更多的 34 pt 外观为基准，独立图标按钮保留 44 pt 点击区；截图/PDF 的保存、分享按钮等高，上下留白对称 | 检查首页更多、网页工具栏、地址弹窗；打开截图和 PDF 预览，比较按钮高度及上下距离。截图上方文字提示与完整图片不能重叠。 |
| 首次使用默认 Pixabay → Nature，默认隐藏网页顶部搜索框 | 在全新安装或测试模拟器检查壁纸来源和主题；搜索后网页顶部不应出现搜索框，底部地址仍能编辑。 |
| 老用户已保存的壁纸来源、主题、顶部搜索框开关和工具栏布局保留 | 更新前记下上述设置（建议先开启顶部搜索框、选其他壁纸来源），直接覆盖安装新版，重开后逐项核对。不要卸载旧 App。 |
| 自定义浏览器工具栏增加阅读模式、资源面板、文件、书库、下载、Wi-Fi 传输、广告过滤 | 设置 → 自定义浏览器工具栏 → 选位置 → 向下滚动选择新工具 → 保存。回到网页点该按钮，确认进入对应工具；重开 App 后布局保留。新选项不会自动占用旧布局。 |
| 地址弹窗打开即选中全部关键词；后续编辑不反复全选 | 搜索「电影」→ 点底部地址 → 直接输入「小说」，应替换而非追加；移动光标、继续输入、改当前网页链接，再次打开弹窗时重新全选。 |

## 样式统一检查（本轮补充）

- **统一主次操作**：工具栏配置、地址弹窗、截图/PDF、站点保存、批量导入、扩展安装、脚本文档和 Wi-Fi 传输使用共用按钮样式。普通字号以 44 pt 为最小高度；大字号允许增高，长文案空间不足时成对按钮自动上下排列。禁用、按下和停止操作的红色状态统一处理。
- **统一图标动作**：资源面板的下载/复制/分享、文件分享、播放器辅助控制采用 34 pt 视觉底座和 44 pt 点击区；阅读工具栏图标按同一基准缩放。播放主操作与系统视频控件保留用途所需的层级。
- **统一页面细节**：地址弹窗、资源面板的强调色跟随页面 tint；移除保存/导入按钮自带的蓝紫渐变，设置页使用系统分组背景。阅读目录/外观使用行内标题、右侧完成按钮，外观面板可展开到大尺寸。

验收建议：依次打开设置 → 站点新增/批量导入/工具栏配置/扩展安装，比较主次按钮；再打开资源面板、文件列表、阅读和播放页面，比较图标与间距。浅色、深色各走一遍；在较窄屏幕与辅助功能大字号下，检查长按钮文案完整、页面可滚动，且底部按钮不互相挤压。切换阿拉伯语等从右向左语言时，检查左右动作顺序。

本次已渲染并检查设置、批量导入、资料库、媒体、资源面板、截图、工具栏配置及地址弹窗等页面，包含 320 pt 窄宽度与辅助功能大字号。样式调整没有修改用户保存的偏好或工具栏动作。

## P0–P5：已有功能完善与测试

以下按 [已有功能完善记录](EXISTING_FEATURES_REVIEW.md) 和对应代码整理，只列实际处理过的问题。

| 阶段 | 已完成的修改 | 建议手动操作与预期 |
| --- | --- | --- |
| P0 体验与响应 | 广告规则内容不变时保留缓存签名，避免启动时无谓失效；下载暂停/继续和站点帮助按钮触控范围扩大到至少 44 pt；长截图两处动画帧等待加入 250 ms 回退，避免网页停止回调后一直等待 | 打开下载页，检查暂停/继续按钮是否容易点击；打开长网页 → 网页工具 → 长截图，确认能结束、底部完整，返回后网页仍可操作。规则缓存与停止动画帧的故障场景由自动化测试覆盖。 |
| P1 自定义 | 修复自动名称不随域名更新、切换分组后仍保存到旧组；新增/编辑统一校验 HTTP(S) 地址和搜索占位符 `%@`，去首尾空白，派生主页保留端口；移除人为 300 ms 保存延时；内置站点名称改为只读；网址错误改为明确提示并补齐 50 语言 | 设置 → 站点管理 → 新增自定义站点，连续修改模板域名，自动名称应跟随；手动改名称后再换域名，名称应保留。切换分组并保存，检查所属组。用 `https://example.com:8443/search?q=%@` 检查保存的主页保留端口；缺少 `%@` 或输入 `javascript:` 地址应无法保存并显示原因。 |
| P2 JavaScript 扩展 | 清空显式站点范围后禁止保存，避免意外扩大为所有网站；编辑与导入统一按 UTF-8 字节限制脚本大小；匹配规则输入关闭自动大写/纠错；保存编辑仍保留原启停状态与脚本存储 | 首页更多 → 脚本扩展，创建仅匹配一个测试站点的脚本，保存后停用。再次编辑，清空站点范围，保存按钮应禁用且原脚本不变；恢复范围并修改代码保存，脚本应仍为停用。启用后只在匹配页面执行。超大脚本应被拒绝。 |
| P3 网页翻译 | 译文只替换仍与请求快照一致的文本；恢复原文或再次翻译不覆盖网页后来更新的内容；跳过实际可编辑区域，包括继承的编辑区域与 `plaintext-only`；文本全部过期时提示失败 | 打开带输入框、可编辑区域和动态正文的测试页 → 整页翻译，输入内容应保持。翻译等待期间或完成后让网页更新正文，再恢复原文/重新翻译，网页新内容不能被旧文字覆盖；普通静态段落应能恢复。动态变化的精确时序已用 WKWebView 自动化样本验证。 |
| P4 广告订阅 | 更新请求返回后保留用户刚切换的开关；拒绝 HTML 登录页/错误页作为规则，更新失败保留上一份有效规则；站点专属元素规则纳入数量统计；规则未变时不改缓存签名 | 网页工具 → 广告过滤管理，更新订阅时关闭其中一项，更新结束和重开 App 后都应保持关闭。使用测试订阅先返回有效规则，再改为 HTML 错误页，更新应报错且原规则保留；含站点专属元素规则的订阅应显示对应数量。请使用测试订阅，避免修改日常规则。 |
| P5 下载与文件入口 | 暂停任务继续保留目标文件名，避免重启后同名下载冲突；迟到的暂停/恢复回调不能改变已完成或已取消任务；失败重试清除旧完成时间和错误；批量清理增加删除确认，无可清理项目时隐藏入口。随后文件入口及查看页改为页面导航 | 下载一个测试文件并暂停，重开 App 后再下载同名文件，两条任务的目标名应不同。完成后打开并核对内容；取消任务后不应自己恢复。失败任务重试后应回到下载中。点批量清理后先取消，记录和文件都应保留；确认清理会删除对应文件，请只用测试文件。再从文件入口打开内容并返回，检查目录保留。 |

这一阶段当时通过 **243 项 XCTest，0 失败（新增 12 项）**；当前 P0–P12 完整回归为下文的 **283 项**，不是两批相加。此前还在专用模拟器实操检查了站点表单、下载清理取消、文件预览和脚本空范围保存。

仍保留的边界：未宣称真机“毫秒级启动”达标；没有删除用户主动安装扩展的通知能力；广告引擎仍为现有 ABP 语法子集及容量限制；Apple 翻译模型、离线语言包和语言组合仍需真机验证。P5 这一轮主要修复下载状态和入口，未据此宣称路线图中的移动/复制、删除恢复、文本编辑等全部任务已完成。后续新增的阅读、压缩与传输能力见 P6–P12。

## P6–P12：从哪里进入

- **文件**：资料库 →「文件」。首页右上角「更多」→「我的下载」后，点右上角文件夹也能进入。
- **书库**：资料库顶部「书库」→「＋」导入。
- **网页阅读**：打开文章或小说网页 → 网页工具菜单 → 阅读模式。
- **网页媒体**：网页资源面板内选择音视频；网页自身播放器也可通过工具菜单调整倍速。

文件列表、子目录、文件查看、书籍阅读、音视频、压缩包预览、压缩设置和 Wi-Fi 传输均采用页面导航。点左上角返回上一级；从悬浮播放器展开再返回应保留原来的目录。

## P6–P12：逐项测试

| 阶段 | 本轮改动 | 建议手动操作与预期 |
| --- | --- | --- |
| P6 媒体 | 网页资源与本地文件共用播放会话；播放、暂停、拖动进度、前后 15 秒、倍速、循环、链接分享；可拖动悬浮播放器 | 打开本地音视频，暂停、拖动、调速；返回文件列表后继续播放；展开浮窗再返回，目录不丢失。换一条媒体，不应同时播放两条。开启循环，等曲末检查重播。 |
| P7 阅读与书库 | TXT 分章/编码选择；PDF 阅读、目录、搜索；书库封面、进度、书签和位置保存；网页正文提取 | 导入中文 TXT 和有文字层的 PDF；翻页、搜索、添加书签、改字号/行距/主题。返回后重开，再退出 App 重开，检查位置恢复。扫描 PDF 无搜索结果时应显示说明。网页阅读返回后检查原网页位置。 |
| P8 压缩解压 | ZIP/7z 创建与加密，ZIP/7z/RAR/RAR5 目录预览和解压；保存目录选择、进度、取消、防覆盖 | 在文件页勾选两个文件 → 底部压缩 → 选格式、目录、密码。打开生成的包，先输入错误密码再输入正确密码，解压并比对内容。已有同名文件应保留；取消后不能留下半成品。 |
| P9 EPUB 与小说 | EPUB 目录、重排、搜索、样式、书签；小说下一页/章、来源展示、重复链接/内容检测 | 导入 EPUB，打开目录、跳章、搜索、改样式并重开。网页阅读中点继续阅读，检查顺序和来源；重复链接应停止，失败后可返回原网页。连续阅读可在阅读外观中关闭。 |
| P10 Wi-Fi | 当前目录的局域网上传/下载页面、二维码、配对码、多文件拖入、进度、取消和重名提示 | 手机与电脑连同一 Wi-Fi；文件页 → 更多 → Wi-Fi 传输 → 开启。电脑打开显示的地址，先试错误码，再正确配对。上传中文文件、下载并比对；同名上传不得覆盖。关闭页面或将 App 切入后台后，连接应停止。 |
| P11 系统播放 | 后台音频会话、锁屏控制、AirPlay、系统画中画接入；0.5–16 倍和 0.1 微调；加载期间暂停不再自动恢复 | 真机播放后锁屏，测试暂停/继续/拖动、耳机拔出和系统打断。连接 AirPlay 接收端；视频进入画中画后返回 App。分别试 0.5、2、8、16 倍，观察实际进度变化和错误提示。 |
| P12 旧电子书 | 按内容识别 MOBI、KF8/AZW3、PalmDOC、Print Replica PDF；预检损坏/受保护文件，保留原件 | 导入无 DRM 的 MOBI、AZW3、AZW4、PRC/PDB 样本；检查正文、目录、图片、书签及重开位置。损坏或受保护样本应提示失败，原文件仍在。 |

## 插画与页面显示检查

新增三张与 HeartCalm 手绘气质协调的插画：整理文件用于搜索历史/下载/文件空状态，读书用于收藏/书库空状态，设备间传递纸飞机用于 Wi-Fi 传输准备页。素材随 App 离线打包，只在相关工具页展示。

手动检查：切换浅色和深色外观，分别打开空收藏、空搜索历史、空下载列表、空文件目录、空书库和 Wi-Fi 传输页，确认插画清楚、文字和按钮可读；已有文件或书籍时仍显示列表。开启 Wi-Fi 后插画应隐藏，二维码和配对码完整显示；横屏或大字号时页面可滚动。素材路径和完整生成提示词见 [插画说明](ILLUSTRATIONS.md)。

## 已执行的检查

本轮额外验证：

- 新增 9 项回归测试：无后缀/错误后缀图片与临时预览清理、文件图标分类、删除越界预检、7 天评价只请求一次、吸附边界、小说净化/短翻页链接、阅读样式不重载，以及免配对码传输的会话和收发反馈。
- 原生 UI 操作已走通文件多选 → 压缩设置/返回、分享面板/关闭、删除取消/确认；Wi-Fi 地址和代码复制；播放器按钮收起、边缘拖动吸附、点击恢复及关闭。仅删除专用模拟器内自行创建的测试文件。
- 真实小说网址已连续读取 4 页；网页测试覆盖 Wi-Fi 正确/错误配对、免配对码、上传、重名、图标和中英文窄屏；官网新图片在中英文和手机/桌面尺寸中均显示正确。
- 回归排查中将音高保持改为 spectral 算法，并保留播放器正常缓冲启动，修复所测样本在 0.5 倍首次启动时的时钟停滞；测试仍核对真实播放时钟。内置脚本默认值测试改为检查新建记录，避免把用户已开启的脚本误报为失败。


- iPhone 17 Pro / iOS 26.2 模拟器：完整自动化回归 **283 项通过，0 失败**（含 23 项工具测试、6 项 UI/偏好回归、9 项完善测试及 2 项全屏/主题回归）；新增测试覆盖阅读引擎、真实格式样本、压缩密码/损坏/路径越界、媒体实际进度、小说循环限制，播放器展开后的目录导航，以及旧设置保留、新工具栏选项存取、关键词全选后继续编辑、大图片预览布局及窄屏/从右向左动作排列。
- Wi-Fi 服务：通过真实本机 TCP 连接测试配对、上传/下载内容一致、同名冲突、非法路径与 HTTP 解析。电脑网页另用模拟响应测试错误码重试、多文件/中文名上传、文件名脚本注入和手机/桌面布局。
- 本次页面显示：在专用模拟器渲染并检查五个资料库页面、深色空收藏、大字号书库、Wi-Fi 准备页、截图/PDF、工具栏配置和关键词全选状态。
- 手动模拟器操作：主页 → 文件 → TXT 阅读/返回、书签和章节跳转、音频播放页及悬浮播放器、图片预览、压缩包目录/保存位置/解压返回。文件相关入口已改成整页。
- 模拟器构建、iOS 真机目标 Debug/Release 无签名编译均通过；原有 50 语言本地化严格检查、50 语言商店 JSON 一致性检查和 Git 空白检查。

**仍需真机验收**：两台设备真实 Wi-Fi 互传、AirPlay 接收端、系统画中画、锁屏/电话打断、音画同步、耗电温升，以及 iOS 17 和其他屏幕尺寸。这些不计作模拟器测试已通过。

## 当前兼容范围

- ZIP/7z 可创建及加密；RAR/RAR5 只解压，不支持创建 RAR、分卷包或 ZIP64。解压上限为 10,000 项、单项 512 MB、总量 1 GB。ZIP 压缩取消会在当前文件处理结束后清理。
- 书籍上限 128 MB。AZW/PRC/PDB 按内部格式判断；目前支持 MOBI/KF8、PalmDOC 和 AZW4 Print Replica PDF。不支持 DRM、KFX 或任意其他 PDB 类型；不能按后缀承诺“Kindle 全格式”。
- 小说拼页依赖网页提供的同源下一页/章链接，每次手动加载，最多加载 30 页/章；登录/付费或提取失败时返回原站，不保证所有网站适配。
- 高倍速受编码、网站播放器及接收端能力限制；0.5–16 倍实际进度已用 WAV 样本验证，不能据此承诺所有媒体或 AirPlay 均支持 16 倍。
- Wi-Fi 单文件上限 512 MB，配对会话 15 分钟；关闭、网络切换或进入后台即停止，不支持断点续传，需重新开始。
- 本轮新增工具文案包含简体中文、繁体中文和英文；其余语言暂回退英文。原有 50 语言文案保留。

第三方源码、固定版本及许可证见 [ThirdParty/README.md](../ThirdParty/README.md)，App 设置中也可查看开源声明。
